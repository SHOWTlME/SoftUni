function solve (arg1, arg2, arg3, arg4){
    let hourStart = Number(arg1);
    let minuteStart = Number(arg2);
    let hourArive = Number(arg3);
    let minuteArive = Number(arg4);

    if (hourStart === hourArive || minuteArive) 
}

solve();