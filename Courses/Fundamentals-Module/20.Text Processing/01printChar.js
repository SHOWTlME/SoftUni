function printCharacters(word) {
    // for (const char of word) {
    //     console.log(char);
    // }
    for(let i = 0; i < word.length; i++) {
        console.log(word[i]);
    }
}
printCharacters('AWord');