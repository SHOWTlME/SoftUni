function sumFirstAndLastArrays(arr) {
    let first = arr[0];
    let last = arr[arr.length - 1];

    console.log(first + last);
}
sumFirstAndLastArrays([20, 30, 40]);
sumFirstAndLastArrays([10, 17, 22, 33]);
sumFirstAndLastArrays([11, 58, 69]);