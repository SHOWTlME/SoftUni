function maxEqualElements (arr) {
    for(let i = 0; i < arr.length; i++) {
        let maxEqual = arr.length[i];
        if (maxEqual) {
            console.log(maxEqual);
        }
    }
}
maxEqualElements([2, 1, 1, 2, 3, 3, 2, 2, 2, 1])